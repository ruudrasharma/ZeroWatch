/** @type {import('next').NextConfig} */
const nextConfig = {
  // Standalone output for a minimal Docker image (docker-compose.yml / DEPLOYMENT.md).
  output: "standalone",
};

export default nextConfig;
