# ZeroWatch ML training — dependencies for ml/scripts/*.py.
# Superset overlaps with backend/requirements.txt (same checkpoint format);
# imbalanced-learn is training-only (SMOTE), not needed at inference time so
# it's deliberately not in backend/requirements.txt.
torch>=2.6.0
scikit-learn>=1.6.1
numpy>=2.1.3
pandas>=2.2.3
joblib>=1.6.0
imbalanced-learn>=0.13.0
