# ZeroWatch backend — Python dependencies
# Python 3.11+ required (per TECH_STACK.md)

# ─── Web framework ────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9       # form data support

# ─── Data validation ─────────────────────────────────────────────────────────
pydantic>=2.9,<3       # 2.14.0b2+ required for Python 3.14 compatibility
pydantic-settings>=2.5

# ─── Database ────────────────────────────────────────────────────────────────
sqlalchemy==2.0.35

# ─── HTTP client (async, for external API calls) ─────────────────────────────
httpx==0.27.2

# ─── Environment ─────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ─── HTML parsing (for tech fingerprinting) ──────────────────────────────────
beautifulsoup4==4.12.3
lxml==5.3.0

# ─── Machine learning ─────────────────────────────────────────────────────────
# NOTE: bumped from torch==2.4.1/scikit-learn==1.5.2/numpy==1.26.4/shap==0.46.0 —
# those pins have no wheels for Python 3.13+, which is what's available on this
# machine (no 3.11/3.12 interpreter installed). Bumped to the nearest versions
# with Python 3.13 wheels; behavior is unchanged for this project's usage.
torch==2.6.0
scikit-learn==1.6.1
numpy==2.1.3
pandas==2.2.3
shap==0.47.2
joblib==1.6.0          # checkpoints (scaler/label_encoders/RF) were saved with
                        # joblib.dump() — must be loaded with joblib.load(), not
                        # raw pickle.load(), or numpy array unpickling corrupts

# ─── PDF export ───────────────────────────────────────────────────────────────
reportlab==4.2.5
# weasyprint==62.3  # optional heavier alternative (requires system libpango)

# ─── Dev / linting ────────────────────────────────────────────────────────────
ruff==0.6.9
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2        # used in tests as async client
